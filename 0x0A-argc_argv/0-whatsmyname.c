#include <stdio.h>
/**
 * main - the function to print the name of a program
 * @argc: the count
 * @argv: the array
 * Return: Always 0 (Success)
 */
int main(int argc, char *argv[])
{
	argc++;
	printf(%s\n, argv[0]);
	return (0);
}
