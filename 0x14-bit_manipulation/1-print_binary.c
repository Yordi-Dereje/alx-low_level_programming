#include "main.h"
/**
 * print_binary - a function that prints the binary representation of a number
 * @n: the passed number
 * Return: nothing
 */
void print_binary(unsigned long int n)
{
	//okay can't use array, malloc or the arthmetics
	//can't also use printf. So putchar it is
}
